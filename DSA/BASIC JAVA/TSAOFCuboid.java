package D1BasicJava;

public class TSAOFCuboid {
    static void main(String[] args) {
        double l = 3, b=5, h = 4;
        double tsa = 2*((l*b) + (b*h) + (l*h));
        System.out.println(tsa);
    }
}
